apiVersion: v1
kind: Namespace
metadata:
  name: vibe-hub
  
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vibe-hub-deployment
  namespace: vibe-hub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vibe-hub
  template:
    metadata:
      name: vibe-hub-pod
      labels:
        app: vibe-hub
    spec:
      containers:
        - name: nginx-container
          image: nginx:alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: volume-html-content
              mountPath: /usr/share/nginx/html
        - name: git-sync-sidecar
          image: registry.k8s.io/git-sync/git-sync:v4.2.3
          args:
            - --repo=git@github.com:LegoCoyMan-Alpha/coy-vibe54-test.git
            - --ref=main
            - --root=/tmp/git
            - --link=content
            - --ssh-known-hosts=false
            - --period=30s
            - --one-time=false
          volumeMounts:
            - name: volume-html-content
              mountPath: /tmp/git
            - name: git-ssh-key
              mountPath: /etc/git-secret
              readOnly: true
      volumes:
        - name: volume-html-content
          emptyDir: {}
        - name: git-ssh-key
          secret:
            secretName: git-ssh-key
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: vibe-hub
    website: Coy
  namespace: vibe-hub
spec:
  type: NodePort
  selector:
    app: vibe-hub
  ports:
  - name: http
    port: 80
    targetPort: 80
